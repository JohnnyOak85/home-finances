{
  "name": "home-finances",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start",
    "containerize": "docker compose down && docker image prune -a -f && docker volume prune -f && docker builder prune -f && docker compose up -d"
  }
}
